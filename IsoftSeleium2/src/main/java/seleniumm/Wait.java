package seleniumm;

public class Wait {
	/* Wait is class and it has 3 types in selenium
	 * 1.implicit wait      2.Explicit(CONDITIONAL) wait    3.fluent wait
	 * 
	 * Fluent wait=== flient wait is similar like Explicit wait
	 * it tells to the webdriver to wait for given time before throwing an Exception for that condition to check.
	 *so, its wait for conditon check for that given time
	 *the diffrence in Explicit wait and fluent wait is, we can set polling time for condition check
	 *its advantage of fluent wait 
	 * 
	 * 
	 * 
	 * 
	 */

}
